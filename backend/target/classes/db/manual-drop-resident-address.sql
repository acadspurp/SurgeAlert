-- After removing `address` from the Java entity, Hibernate no longer INSERTs that column.
-- If MySQL still has `address` NOT NULL without default, registration fails with:
--   Field 'address' doesn't have a default value
--
-- Pick ONE:
--
-- Option A — allow NULL for legacy column (quick fix; new rows get address = NULL):
ALTER TABLE residents MODIFY COLUMN address VARCHAR(255) NULL;
--
-- Option B — remove column entirely (preferred long-term):
-- ALTER TABLE residents DROP COLUMN address;
