# Shared environment template for local dev and hosting
# Copy this file to ".env" and replace placeholders with real values.
# IMPORTANT: You MUST manually create the database "surgealert_db" in MySQL before running.

# --- Core backend ---
SERVER_PORT=8080
SPRING_DATASOURCE_URL=jdbc:mysql://localhost:3306/surgealert_db?useSSL=false&serverTimezone=Asia/Manila&allowPublicKeyRetrieval=true
SPRING_DATASOURCE_USERNAME=your_db_user
SPRING_DATASOURCE_PASSWORD=your_db_password
SURGE_ENCRYPTION_SECRET=replace_with_16_plus_char_secret

# --- SMTP / email ---
SPRING_MAIL_USERNAME=your_email@gmail.com
SPRING_MAIL_PASSWORD=your_gmail_app_password

# --- External APIs ---
WORLDTIDES_API_KEY=your_worldtides_key
SURGE_TIDES_CACHE_MAX_AGE_DAYS=2
SEMAPHORE_ENABLED=false
SEMAPHORE_API_KEY=your_semaphore_api_key
SEMAPHORE_API_URL=https://api.semaphore.co/api/v4/messages
SEMAPHORE_SENDER_NAME=SurgeAlert

# --- MQTT ---
MQTT_BROKER_URL=tcp://localhost:1883
MQTT_BROKER_USERNAME=your_mqtt_username
MQTT_BROKER_PASSWORD=your_mqtt_password
MQTT_CLIENT_ID=surgealert-backend
MQTT_TOPIC_SENSOR=sensor/data
MQTT_USE_SSL=false



# --- Edge system ---
BACKEND_IP=127.0.0.1
BACKEND_PORT=8080
EDGE_API_KEY=replace_with_same_app_edge_api_key
MQTT_BROKER=localhost
MQTT_USERNAME=your_mqtt_username
MQTT_PASSWORD=your_mqtt_password
SMS_ONLINE_PRIMARY=true
SMOOTHING_WINDOW=5
MAX_DELTA_M_PER_CYCLE=0.75
CV_MIN_FLOW_MPS=0.05
CV_MIN_RISE_MPS=0.005

# --- Manual canary governance ---
SURGE_CANARY_ENABLED=false
SURGE_CANARY_PERCENTAGE=5
SURGE_CANARY_SENSOR_ALLOWLIST=sensor-a,sensor-b

# --- Frontend (non-secret, safe for browser bundle) ---
<<<<<<< HEAD
=======
# Local: backend on default port. Production (Cloudflare Pages): set to your Render URL + /api, e.g.
# VITE_API_BASE_URL=https://surgealert-backend-fxqk.onrender.com/api
# Alternatively set origin only: VITE_BACKEND_ORIGIN=https://surgealert-backend-fxqk.onrender.com
>>>>>>> parent of b6ead82a (.)
VITE_API_BASE_URL=http://localhost:8080/api
